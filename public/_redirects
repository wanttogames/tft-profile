# Use Cloudflare Pages default SPA fallback.
# Do not rewrite /privacy or /terms to /index.html: HTML canonicalization can lose the requested path.
# Keep this file to replace obsolete rules when extracting an update ZIP.
